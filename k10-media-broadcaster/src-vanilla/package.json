{
  "name": "k10-media-broadcaster-vanilla",
  "private": true,
  "version": "1.0.0",
  "description": "TypeScript/Vite build for vanilla dashboard (dashboard-build.html)",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "vite",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
